/* ============================================================
   THE ROM-COM ARCHIVE — Configuration
   ============================================================

   This site pulls REAL movie posters and REAL cast photos from
   The Movie Database (TMDB) — a free, licensed film API used by
   thousands of production websites.

   HOW TO GET YOUR FREE API KEY (takes about 2 minutes):
   1. Go to https://www.themoviedb.org/signup and create a free account.
   2. Once logged in, go to https://www.themoviedb.org/settings/api
   3. Click "Request an API Key" -> choose "Developer" -> fill the short form
      (you can put "personal portfolio project" as the use case).
   4. Copy the "API Key (v3 auth)" value TMDB gives you.
   5. Paste it below, replacing YOUR_TMDB_API_KEY_HERE.

   Without a key, the site still works perfectly — it just falls
   back to the stylized placeholder artwork instead of real photos.

   TMDB requires attribution — a small "Data & images from TMDB"
   credit is already included in the site footer. Do not remove it.
   ============================================================ */

const TMDB_API_KEY = "YOUR_TMDB_API_KEY_HERE";
